# Selective review response: fixed scope and first-attempt interpretation

## Accepted scientific correction

The attached review's central objection is supported by current code and native tests: diagnosis exposes one distinct legal raw array. Repeated reads change the artifact reference, not the array; alternate starts/channels/handles are rejected. The single-read Graph property is retained as a conditional source fact but removed as the Abstract/Introduction's candidate multi-observation loss mechanism. No new task or data permission is introduced to rescue it.

The revised question concerns prescribed numerical execution, premature submission and opportunities after prediction. Global access to an action, actual selection, changed final label/support and improved correctness are separate observations. New artifact IDs alone do not prove additional numerical evidence. We do not introduce an unvalidated evidence-sufficiency estimator.

The manuscript specifies common released scope and potential experts/fitted parameters, not identical temporal tool access or identical executed computations. No-submission handling and the actual class-stratified bearing bootstrap are stated explicitly. The two diagnosis contrasts are primary; replay and process quantities are secondary. Conditional submitted-only or interruption-free results are descriptive, not replacements for all first attempts.

## Existing implementation reused, not reapplied

Baseline inspected: Benchmark 7d9132e, P02 28c5cae. The earlier unpushed candidate was superseded by the native block-first-attempt implementation and its tests. Preserve `execution_plan.py`, `asset_trial_first_attempt_v1`, `outcome_selection=first_attempt`, `remaining_request_reserve` and shared fitted references. Do not add `blocking.py`, rename the schedule, or repeat the former candidate's entire patch.

Two narrow additions address remaining execution ambiguities. The optional cyclic order balances treatment positions to within one occurrence; independent legacy order remains available. Request intent is persisted in the existing provider log before transmission; unresolved request/canonical evidence prevents automatic resending. This is conservative refusal, not a ledger, a second event authority or exactly-once remote execution. Complete canonical failures remain valid first outcomes and are not replaced by later successes.

The prospective YAML retains the existing schedule but uses a new balanced-v2 study/output. Old independent/cellwise outputs are not resumed or pooled under changed conditions. The reserve remains per actual unattempted block, not a second global formula or an automatically enlarged spending limit.

## Choices not adopted as extra machinery

No new router, state, placebo factorial, uncertainty estimator, task score or numerical representation was added. Position balance does not guarantee balance by class or control carryover; first-attempt preservation does not solve provider nonstationarity. A log intent may precede a request never sent, and simultaneous loss of log and bundle is undetectable by this check. Confirmatory frozen-plan validation may resolve data before the recovery guard, but neither fitting nor provider transmission precedes it.

Process and endpoint decomposition are specified for the future canonical real outcomes. This continuation does not claim a new empirical continuation rate, label-change rate, conditional Macro-F1 or confusion matrix. Those still require completed task records; no fixture plot is presented as performance.

## Primary literature inspected

StateFlow v5 §§3.1/4.1 and its state ablation text explicitly contain state-dependent instructions, refined ReAct, a genuine Verify step, and remaining exploration after Observe removal. These are direct precedents. Source: https://arxiv.org/html/2403.11322v5 .

PHMForge's available full HTML Appendix H contains verification, distractor and discovery controls. We use that inspected content, not an inferred absence from an abstract or an unavailable version. Source: https://arxiv.org/html/2604.01532 .

The original tau-bench abstract explicitly motivates repeated-trial reliability and pass^k. Only that scope is used; this inspection did not establish its detailed retry or failure-selection implementation. Source: https://arxiv.org/abs/2406.12045v1 .

No reported external scores are same-protocol baselines here. The information and policy-inclusion statements are standard specializations. Scientific novelty still requires structured PHM findings, positive or negative, with active treatment exposure.

## Actual verification

Code commit ae5c80e8; existing Actions run **35314128747** was downloaded and inspected. **205 installed research tests passed in 24.208 s**, including all **nine new safety/order tests**. Portable production checks: **52 passed, one local-data acceptance deselected, 12 subtests, 0.55 s**. This is not complete real-data acceptance. Prior baseline196 tests are not new work.

The new evidence includes 42 deterministic order cases (1–21 bearings, two seeds), all with maximum position imbalance at most one. Native synthetic four-arm execution retains independent agents and one numerical fit; a recorded provider failure remains the same first outcome after continuation. A subprocess exits abruptly with code87 after the persisted request-start record and before any real network call. Orphan/partial record cases are refused, not repaired into invented outcomes.

Local preliminary checks used source imports on Python3.13, outside the package's supported installation range; the subsequent ordinary Python3.12 installed CI is the authoritative validation. No source-loader substitute was used. Real HDF5 reads, live model responses and new PHM task effects remain zero.

## Remaining scientific gate

The revised manuscript is 2,152 whitespace-delimited words, versus the current remote draft's2,851; this is an editing count. The unique manuscript, theory07 and experiment mapping now point to the actual native implementation. First-attempt scheduling is identification infrastructure, not P02's main algorithmic novelty.

Real waveform correspondence, native diagnosis/replay Scripted acceptance and an approved four-arm provider cohort remain required. Keep both migration PRs Draft until genuine local acceptance. Pure horizon and dynamic revision are separate uncompleted hypotheses; no such efficacy claim appears in the revised abstract.
