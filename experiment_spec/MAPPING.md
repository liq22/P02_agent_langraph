# Paper 2 → Benchmark

The scientific experiment specification is `paper/experiments/EXPERIMENT_MATRIX.md`; the manuscript is `paper/draft/main.md`. Execution and the local handoff live only in Benchmark.

| Role | Benchmark configuration / implementation | Paper outlet |
|---|---|---|
| Primary | `configs/paper02_graph/indexing.yaml`; existing component factory, `graph-indexing` conditions | Main Sections 3–5; actual outcomes in Section 6 |
| Secondary original system | `configs/paper02_graph/main.yaml` | Separate Graph-minus-Generic comparison |
| Diagnostic earlier prompts | `components.yaml`, `organization.yaml` in the same config directory | Original semantic cue/package definitions retained; no new-control reuse |
| Supplementary menu identity | `mask_relevance.yaml`, `cardinality_control.py` | Existing count-matched comparison, not a new indexing effect |
| Source boundaries | `state.py`, `agent.py`, `test_graph_indexing.py` | Theory07: history sufficiency, menu classes, single acquisition |
| Data availability | `experiments/graph_control/inspect_metadata.py` | Metadata-only report; no scientific task effect |
| Actual execution | `paper/goals/P02_MATCHED_CONTROL.md` | HDF5 and same-study verification, then approved model cohort |

Compact current evidence: Benchmark `results/graph_control/indexing_20260918/{metadata.json,menus.csv,README.md}`. Installed run 35300832663 passes the scoped portable suite and parses the upstream workbook. One unchanged local-data test is explicitly outside portable CI and remains required before the migration merges. Neither metadata nor synthetic/request fixtures establish PHM benefit.

All old prompt families and historical results retain their original semantics. No second Runner, raw-result tree, loader or evaluator is created here. Pure nested-prefix horizon, active dynamic revision and repeated reliability need their actual studies; indexing does not establish them. Paper 0 receives measured G task/cost findings only after those exist.
