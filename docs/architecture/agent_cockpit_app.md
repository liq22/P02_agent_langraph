# Historical Agent Cockpit documentation

The former backend/web cockpit is not part of the paper-only P02 repository. Its implementation and deployment instructions are historical Git content, not a current execution path.

Use `paper/goals/README.md` for this paper and `experiment_spec/MAPPING.md` for the single Benchmark implementation. Do not recreate a gateway, frontend, scheduler or node-local manuscript system to run Graph experiments.
