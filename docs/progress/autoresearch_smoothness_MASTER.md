# Autoresearch Smoothness Progress

## Status

Active implementation.

## Completed

- Planned bootstrap readiness API, run-once API, session restore, projection fixes, web cockpit UX, docs, and tests.

## Acceptance

- Bootstrap returns actionable setup state even when graph files are missing.
- Run is disabled until graph, config, target, and prompt are ready.
- One bounded run can be launched with one frontend action.
- Local skill and prompt files are visible in node details without changing minimal graph schema.
