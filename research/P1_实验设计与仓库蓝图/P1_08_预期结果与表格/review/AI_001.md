# External Re-Review: P1_08 Expected Results And Tables

reviewer_id: `external_reviewer_codex_p1_08_rereview_2026-05-05`

## Verdict

- Score: 88/100
- Pass/fail: pass for node progression
- Hard fail: false
- Downstream ready for node progression only: true

## Findings

No blocking findings remain.

1. Prior hard fail `P1_08_REVIEW_F001_SCHEMA_INVALID_C1_SUPPORT_STATUS` is resolved.
   - Severity: resolved
   - claim_id: `c1`
   - evidence_id: `P1_08_E002_P1_05_HYPOTHESIS_STATUS`
   - Location: `artifacts/claim_evidence_registry.yaml:6-23`, `artifacts/claim_map.yaml:6-14`, `artifacts/table_plan.yaml:30-36`, `docs/manuscript.md:22-24`.
   - Review: the local registry now keeps repository-facing `support_status: weak` while preserving parser-facing `protocol_support_status: supported_limited`. The claim map keeps `support_status: supported_limited` and adds `submission_support_status: weak`, which preserves the P1_07 parser meaning and avoids the repository-wide registry schema-invalid value in the required registry field.
   - Actionable fix: none required for this node.

2. Prior hard fail `P1_08_REVIEW_F002_TABLE_COLUMN_CONTRACT_INCOMPLETE` is resolved.
   - Severity: resolved
   - claim_id: `c1`, `c2`, `c3`, `c4`, `c5`
   - evidence_id: `P1_08_E001_P1_07_PROTOCOL_MAP`, `P1_08_E002_P1_05_HYPOTHESIS_STATUS`, `P1_08_E003_P0_05_CONSTRAINT_RISK`, `P1_08_E005_TABLE_PLAN`
   - Location: `artifacts/table_plan.yaml:48-70`, `artifacts/table_plan.yaml:100-123`, `artifacts/table_plan.yaml:170-189`, `artifacts/table_plan.yaml:211-224`.
   - Review: every planned table schema now exposes parser-facing `claim_id`, `evidence_id`, `source_artifact`, `support_status`, `boundary_label`, and `failure_or_limitation_ref` or an equivalent formal failure field. The negative/unclear ledger uses `claim_id` and `support_status`, retaining `affected_claim_id` only as explanatory metadata.
   - Actionable fix: none required for this node.

## Compatibility Checks

- Each planned table has named claim IDs and table refs through `table_plan.yaml` and `claim_map.yaml`; evidence IDs are explicit in the claim map, local registry, and negative ledger entries.
- P1_09 `c1` compatibility remains preserved: P1_08 maps `c1` to `fig_main_synthetic_signal`, and P1_09 still bounds that figure to a synthetic/offline single-run signal, not a formal paper result.
- The package avoids claiming observed AutoResearch improvement, PHMGA formal eligibility, selected backend lock, Stage C/D success, RM101 resolution, or final submission readiness.
- Negative, rejected, unclear, low-power, and blocked evidence remains visible in `negative_unclear_result_ledger`, `failure_register.yaml`, `negative_result_note.md`, and `keep_discard_ledger.yaml`.

## Residual Risks

- This pass is for P1_08 node progression only. P0_05 still records final-submission blockers outside this node, including global registry schema blockers and final review-threshold gaps.
- Future formal rows still need actual `result_md`, `artifact_dir`, ledger rows, repeat counts, uncertainty intervals, and Stage C/D gate passes before any result-level claim can be strengthened.
