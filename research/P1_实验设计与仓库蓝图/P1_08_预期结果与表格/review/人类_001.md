# P1_08 Human Review — Slot 人类_001

- reviewer_id: claude_code_teammate_delegate_p1_08_human_review_2026-05-05
- reviewer_role: user-authorized Claude Code teammate delegate
- delegation_basis: User explicitly authorized Claude Code/Anthropic teammate review for the P1_08 human-review slot.
- transparency_notice: This review was performed by a Claude Code teammate delegate, not a biological human. Codex remains the lead and final gate owner. This review does not claim submission-ready status.

## Verdict

- overall_verdict: pass
- score: 87/100
- hard_fail: false
- scope: P1_08 node progression only; not a final submission-readiness verdict.

## Review Questions

### Q1: Does each planned table support a named claim, and are claim IDs/evidence IDs explicit?

**Finding: Yes.**

All four planned tables map to named claims with explicit claim IDs and evidence IDs:

| Table | Claim IDs | Evidence IDs |
|-------|-----------|--------------|
| main_results | c1, c5 | P1_08_E002, P1_08_E005 |
| formal_eligibility_gate | c2, c4 | P1_08_E001, P1_08_E003, P1_08_E006 |
| ablation_and_efficiency_results | c3, c5 | P1_08_E001, P1_08_E005 |
| negative_unclear_result_ledger | c4, c5 | P1_08_E002, P1_08_E003 |

Cross-artifact consistency: `table_plan.yaml`, `claim_map.yaml`, and `claim_evidence_registry.yaml` agree on all claim→evidence bindings. Every claim row includes `claim_id`, `evidence_id`, `source_artifact`, `support_status`, `boundary_label`, and `failure_or_limitation_ref`.

### Q2: Are all required columns present for main results, formal eligibility rows, ablation/efficiency rows, uncertainty, and negative/unclear evidence?

**Finding: Yes, with one advisory.**

- `main_results` (23 columns): includes claim_id, evidence_id, metric_id, direction, numerator, denominator, point_estimate, confidence_or_bootstrap_interval, repeat_count, support_status, boundary_label, failure_or_limitation_ref, plus governance-specific rate metrics. Complete.
- `formal_eligibility_gate` (24 columns): includes formal_row_id, stage, dataset_id, backend_id, provider_name, model_name, provider_model_policy_pass, metadata_h5_alignment_pass, artifact_contract_pass, selected_global_best_backend_locked, result_md, artifact_dir, repeat_count, formal_eligibility_status, failure_or_reject_evidence_ref. Complete.
- `ablation_and_efficiency_results` (18 columns): includes changed_factor, unchanged_controls, primary_metric_delta, budget_minutes, llm_calls, token_budget, repeat_count, uncertainty_interval. Complete.
- `negative_unclear_result_ledger` (15 columns): includes severity, interpretation, action, retained_location, downgrade_rule, denominator_included. Complete.

**Advisory:** The manuscript states "Rate metrics must include numerator, denominator, direction, repeat count, and uncertainty interval field." The `ablation_and_efficiency_results` table has `repeat_count` and `uncertainty_interval` but does not explicitly list `numerator`, `denominator`, or `direction` columns. This is acceptable because ablation rows carry `primary_metric_delta` instead of rate-decomposition fields, but a downstream parser may need a note clarifying that ablation rows use delta semantics rather than rate semantics.

### Q3: Are negative, rejected, unclear, blocked, low-power, and missing formal rows given first-class table locations rather than hidden?

**Finding: Yes.**

- `negative_unclear_result_ledger` is a dedicated first-class table with four concrete entries:
  - P1_08_NEG_001: real-data generalization unsupported (boundary: limitation)
  - P1_08_NEG_002: RM101 resolution unsupported (boundary: reject_evidence)
  - P1_08_NEG_003: variance stability unclear (boundary: limitation)
  - P1_08_NEG_004: schema and review threshold blockers (boundary: blocker)
- `failure_register.yaml` records four failures (F001–F004) with severity, interpretation, and action.
- `negative_result_note.md` states the strict interpretation rule: missing alignment/backend/stages/repeats/uncertainty → row stays as blocker or limitation.
- `keep_discard_ledger.yaml` explicitly discards a success-only table variant and accuracy-only column set.
- Failure policy confirms: `hidden_negative_results: false`, `limitations_promoted_to_positive_claims: false`, `denominator_exclusion_without_rationale_allowed: false`.

### Q4: Does the package preserve the P1_09 downstream c1 figure reference while narrowing it to a limited synthetic/offline sanity-check claim?

**Finding: Yes.**

- `claim_map.yaml` c1 has `figure_ref: fig_main_synthetic_signal`, matching the downstream P1_09 figure manifest.
- c1 `boundary_label: preliminary_synthetic_only` and `allowed_wording: limited synthetic/offline sanity-check signal` correctly narrow the claim.
- c1 `forbidden_wording` blocks formal performance improvement, real-data generalization, selected backend, RM101 resolution, and submission-ready result.
- `mapping_checks.downstream_c1_ref_preserved_for_p1_09: true` confirms the downstream reference is maintained.

### Q5: Does the package avoid claiming observed AutoResearch improvement, PHMGA formal eligibility, selected backend, Stage C/D success, RM101 resolution, or final submission readiness?

**Finding: Yes.**

- `docs/manuscript.md` Current Conclusion explicitly disclaims all six categories.
- `table_plan.yaml` `table_plan_policy` sets `no_observed_formal_claim: true`, `no_selected_backend_claim: true`, `no_stage_c_or_stage_d_success_claim: true`, `no_rm101_resolution_claim: true`.
- Every claim in `claim_map.yaml` has explicit `forbidden_wording` lists that block overclaiming.
- AI_001 re-review confirms the package avoids all six overclaiming categories.

## Cross-Artifact Consistency Checks

1. **claim_type discrepancy (c3):** `claim_map.yaml` labels c3 as `claim_type: limitation`, while `claim_evidence_registry.yaml` labels c3 as `claim_type: method`. The claim text and evidence bindings are identical; this is a labeling inconsistency, not a semantic one. **Severity: advisory.** Codex may normalize at node-close.

2. **Missing evidence entry (E004):** `table_plan.yaml` references `P1_08_E004_P1_09_DOWNSTREAM_FIGURE_REF` in upstream_evidence, but this evidence_id has no corresponding entry in `claim_evidence_registry.yaml`'s evidence list. It is not used as direct evidence for any claim, so the gap is non-blocking. **Severity: advisory.**

3. **Dual vocabulary bridge:** The `support_status` bridge (parser: `supported_limited` / registry: `weak`) is correctly implemented across all four artifacts (manuscript §Required Columns, claim_map, claim_evidence_registry, table_plan). The prior AI-review hard fail on this point is resolved.

## AI Review Consistency

AI_001 (external re-review, score 88/100, verdict pass, hard_fail false, independence_confirmed true) is consistent with this review's findings. Both prior hard fails (schema-invalid c1 support_status and incomplete table column contract) are confirmed resolved.

## Residual Non-Node Blockers

This pass is for P1_08 node progression only. The following blockers remain outside this node's scope and are not solved by this human review:

- Final-score thresholds not met by node-level pass alone.
- Existing claim-evidence registry schema errors outside P1_08 remain unresolved.
- Selected backend remains pending (P1_08 formal_eligibility_gate records backend lock as a required gate, not a completed one).
- RM101 Stage B reject evidence remains unresolved (P1_08_NEG_002 retains it as reject_evidence).
- PHMGA dirty worktree protection (dirty/untracked entries block future pointer updates).
- PHMGA/Vibench adapter preflight incomplete (formal_eligibility_gate Stage B rows are preflight-only).
- Downstream Stage C main-result rows not executed or passed.
- Downstream Stage D ablation rows not executed or passed.
- Broader unfinished graph nodes retain their own open gates.

## Recommendation

**Pass for P1_08 node progression.** No hard fails, no blocking issues. Two advisory notes (c3 claim_type label discrepancy, E004 missing evidence entry) are minor and can be normalized at node-close.
