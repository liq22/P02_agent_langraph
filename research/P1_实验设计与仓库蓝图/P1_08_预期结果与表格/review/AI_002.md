# AI-002 Final-Threshold Score Re-Review: P1_08 Expected Results And Tables

review_id: `AI-002`
reviewer_agent_id: `final-threshold-reviewer-p1-08-002`
review_type: `score-only node-local final-threshold re-review`

## Scope And Independence

I reviewed P1_08 as a distinct score-only final-threshold reviewer, not as the current author and not as the user. This review is limited to node-local expected-results/table-package score readiness after the addition of `artifacts/table_final_threshold_contract.yaml`. It is not a global submission-readiness review.

Forbidden files were not read. I did not read `.env*`, credentials, tokens, private keys, `_reference/**`, any `docs/HUMAN_ONLY.md`, data directories such as `/mnt/k/D01_vibench`, generated Canvas/report/vendor assets, or any external provider/API material. No external provider/API call was made. No status, checklist, P3 action, current gate, global audit, artifact, manuscript, or acceptance-checklist edits were made.

## Verdict

- Overall score: 91/100
- Overall verdict: pass
- Hard fail: false
- Final submission threshold met for this node-local score-only review: true
- Downstream ready within P1_08 scope: true
- Global submission ready: false

## Findings

No P1_08-scope blockers remain for the expected-results/table-package score review.

1. Prior hard fail on the c1 support-status schema bridge is resolved.
   - `claim_evidence_registry.yaml` uses repository-facing `support_status: weak` for c1 while preserving `protocol_support_status: supported_limited`.
   - `claim_map.yaml` preserves the parser-facing `support_status: supported_limited` and adds `submission_support_status: weak`.
   - `table_plan.yaml` documents the bridge rule, so the limited synthetic signal remains parser-compatible without violating the registry vocabulary.

2. Prior hard fail on table common-row parser fields is resolved.
   - Every planned table exposes claim/evidence/source/status fields.
   - Result and ablation rows include parser-facing metric direction, numerator, denominator, repeat, uncertainty, support status, boundary, and failure/limitation fields where applicable.
   - The formal eligibility table carries the expected provider, metadata-H5, artifact, backend, stage, result path, repeat, eligibility, support, boundary, and failure/reject fields.

3. The new final-threshold contract is sufficient for a node-local score increase.
   - The contract limits the positive claim to documented, consistent, complete, parser-exercisable table-package readiness.
   - It explicitly forbids observed AutoResearch improvement, formal PHMGA result claims, selected backend evidence, RM101 positive resolution, Stage C/D pass evidence, and final submission readiness.
   - It keeps global blockers outside this review, including P1 checklist fields, P1_09/P3/P4 scores, P3_04 action statuses, selected backend, RM101 positive evidence, adapter preflight, and Stage C/D rows.

4. Negative and unclear evidence has first-class table placement.
   - `negative_unclear_result_ledger` is required, not optional.
   - The failure register, negative-result note, and keep/discard ledger retain unsupported real-data generalization, unsupported RM101 resolution, unclear variance stability, blocker rows, and success-only table rejection.

5. P1_09 c1 downstream compatibility is preserved.
   - P1_08 keeps `c1` mapped to `fig_main_synthetic_signal`.
   - P1_09 keeps that figure bounded as a synthetic/offline single-run signal, not a formal paper result.

6. Prior AI/H review comments are responded.
   - `review/response.yaml` records responses for `AI-001` and `H-001`.
   - The former advisory gaps are now normalized: c3 is a method claim in both claim artifacts, E004 is present in the evidence registry, and ablation rows expose direction/numerator/denominator.

## Residual Non-Blocking Limits

The score increase is justified only within the P1_08 table-package boundary. P1_08 still does not provide observed formal results, selected backend evidence, RM101 positive evidence, Stage C/D passes, or repository-wide final-validator success. Checklist/status/P3 action closures remain outside this review.
