# P1_08 Negative And Unclear Result Note

P1_08 treats negative, rejected, unclear, low-power, and blocked evidence as table content rather than hidden cleanup work. The expected tables include a dedicated `negative_unclear_result_ledger` so that failed or missing formal rows remain visible in the denominator.

The current evidence boundary is limited. P1_05 records one supported-limited synthetic/offline signal, two unsupported interpretations, and one unclear stability interpretation. The unsupported interpretations are real-data generalization and RM101 resolution. The unclear interpretation is variance stability. These records are limitations, not positive formal results.

The interpretation rule is strict: if metadata-H5 alignment, selected backend, artifact contract, Stage C rows, Stage D rows, repeat count, uncertainty interval, or result artifact paths are missing, the row remains a blocker, reject-evidence row, or limitation. This is an alternative explanation guard against data accident, implementation bias, metric bias, and cherry-picked success-only tables.

No table in this node claims observed AutoResearch improvement, PHMGA formal eligibility, selected backend, Stage C/D success, RM101 resolution, or final submission readiness.
