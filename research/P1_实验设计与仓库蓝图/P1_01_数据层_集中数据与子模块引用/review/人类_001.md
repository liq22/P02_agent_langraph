# 人类_001

- comment_id: H-001
- status: resolved
- reviewer: Claude Code teammate delegate (user-authorized, not a biological human)
- review_date: 2026-05-04
- final_human_recommendation: pass

## Reviewer Identity and Transparency

This review was performed by a Claude Code teammate delegate acting under explicit user authorization for the P1_01 human-review slot. Codex remains the lead and final gate owner. This review does not claim submission-ready status.

## Review Scope

Reviewed the full bounded packet specified in `review/human_reviewer_prompt.md`: `README.md`, `status.yaml`, `docs/manuscript.md`, all `artifacts/` YAML and Markdown files, `prompts/review_rubric.yaml`, `prompts/acceptance_checklist.yaml`, `review/AI_001.md`, `review/verdict.yaml`, and `review/external_review_handoff.md`. Did not read `docs/HUMAN_ONLY.md`.

## Response to Human Review Questions

### Q1: Is the data/source boundary clear enough for a downstream reader to understand where PHMGA evidence comes from?

Yes. The three-tier truth model is explicit and inspectable:

- **Reading tier**: `artifacts/vibench_data_factory_binding.yaml` specifies the exact component (`src/data_factory`), lists five allowed read-only operations, and marks Vibench DataLoader/sampler/trainer/evaluator as `not_formal_truth`.
- **Implementation tier**: `artifacts/data_reading_boundary.yaml` defines a complete `phmga_owns` list (split, window, DAG, bridge, ML/Torch, metrics, reports, ledger, tables) and a `hard_fail_if` list covering bypass scenarios.
- **Paper tier**: `artifacts/phmga_data_protocol_handoff.yaml` specifies what PHMGA consumes (metadata path, H5 path, dataset name, sample ID) and what it generates (DatasetProtocol, SplitManifest, WindowSpec, validated DAG, feature pipeline, metrics, final report).

A downstream reader can trace from `DATA_ROOT` → Vibench read bundle → PHMGA DatasetProtocol → formal artifacts → paper claims without ambiguity.

### Q2: Are PHM-Vibench and PHMGA responsibilities separated without creating a second source of truth?

Yes. `artifacts/data_reading_boundary.yaml` enforces a one-directional handoff: Vibench produces a read-only bundle, PHMGA consumes it and owns all downstream protocol and result generation. The boundary artifact's `hard_fail_if` section explicitly guards against Vibench DataLoader being used for formal results, Vibench sampler defining PHMGA splits, or PHMGA DatasetProtocol being bypassed. `artifacts/vibench_data_factory_binding.yaml` confirms the `handoff_target: PHMGA DatasetProtocol`. No second source of truth is created.

### Q3: Are the latest Stage B results represented honestly, especially the RM101 reject evidence and pending backend selection?

Yes. `artifacts/result_source_map.yaml` records:

- Ottawa OpenRouter Nemotron: accept, macro-F1 0.8775
- Ottawa BigModel GLM-4.7: accept, macro-F1 0.7227
- RM101 OpenRouter Nemotron: reject, macro-F1 0.1834, reason "max_iterations reached before finish with need_replan"
- RM101 BigModel GLM-4.7: reject, macro-F1 0.1893, reason "max_iterations reached before finish with need_patch"

The `current_known_blocker` section states `selected_global_best_backend_locked: false` and `stage_b_backend_evidence_state: partial_ottawa_accept_rm101_reject`. The `paper_result_claim_policy` is set to `block_main_result_claims_until_selected_backend_and_stage_c_or_d_rows_pass`. `artifacts/failure_register.yaml` keeps F-P02-RESULT-001 (selected backend not locked) and F-P02-RESULT-002 (RM101 Stage B reject evidence) as `status: open`. `artifacts/negative_result_note.md` explicitly states that partial Stage B evidence must not be promoted. This is honest and complete.

### Q4: Are any positive result claims overstated relative to the available ledger and artifacts?

No. Manuscript section 6 ("当前不允许声称的内容") enumerates six explicit prohibitions against premature claims. C-P02-RESULT-001 is classified as `claim_type: limitation` with `action: document_limitation` in `artifacts/claim_evidence_registry.yaml`. No claim in the registry has an overstated `support_status`. All five registered claims map to concrete evidence artifacts. The node correctly refrains from claiming formal experimental results.

### Q5: Is the independent AI review credible and responsive to `prompts/review_rubric.yaml`?

Yes. The external AI review (review/AI_001.md, EXTERNAL-AI-001) scored 88/100, which exceeds the `downstream_pass_min_score: 80` threshold in the rubric. The reviewer independence is confirmed (`independence_confirmed: true`, reviewer agent distinct from `codex-local`). Dimension scores are consistent with the rubric weights. The three non-blocking comments (AI-001 through AI-003) are substantive: pass confirmation, PHMGA adapter preflight reminder, and result source map maintenance requirement. The review mapped concrete claim IDs and artifact paths and correctly identified the downstream blockers. No hard fail was found.

### Q6: Should the node enter fix/close, or must it revise before downstream graph progression?

The node should close the human review gate. It satisfies its bounded role as a data/provenance/submodule boundary package. The AI review gate is passed (verdict.yaml: `overall_verdict: pass`, score 88, `hard_fail: false`, `independence_confirmed: true`). All five claims have concrete evidence mappings. Negative results and open failures are preserved. The acceptance checklist and rubric requirements are met within the node's scope.

## Positive Result Claim Status

Positive result claims remain **blocked**. Specifically:

- `selected_global_best_backend` is pending (RM101 Stage B rows are reject evidence, blocking backend selection).
- Stage C main-result rows have not passed.
- Stage D ablation rows have not passed.
- C-P02-RESULT-001 correctly records this as a limitation, not a supported positive claim.

These are downstream execution blockers, not P1_01 review gate blockers. Closing the P1_01 human review gate does not resolve them.

## Non-Node Blockers (Separate from P1_01 Review)

Even with a pass recommendation on this P1_01 human review, the following non-node blockers remain unresolved:

1. **Selected backend**: `selected_global_best_backend` remains pending because RM101 Stage B free-model rows are reject evidence.
2. **RM101 Stage B reject evidence**: Both RM101 candidates (Nemotron, GLM-4.7) failed with macro-F1 below 0.19 and max-iterations exhaustion.
3. **Stage C/D rows**: No Stage C main-result or Stage D ablation rows have passed; formal paper tables remain empty.
4. **PHMGA adapter preflight**: Full sample-level metadata-H5 alignment still requires PHMGA/Vibench adapter verification.

None of these are solved by the P1_01 human review gate.

## Comments

- comment_id: H-001
- status: resolved
- comment: Pass. The P1_01 node satisfies its data/provenance/submodule boundary role. Data lineage, Vibench-PHMGA responsibility split, claim-evidence registry, failure register, negative result preservation, and honest Stage B evidence representation are all inspectable and consistent. Positive result claims remain correctly blocked pending downstream execution. The node may close the human review gate.

- comment_id: H-002
- status: non_blocking
- comment: The `required_questions_answered` and `required_outputs` items in `prompts/acceptance_checklist.yaml` still show `status: pending` even though the artifacts exist and pass inspection. This is a bookkeeping gap, not a content deficiency. Recommend updating checklist statuses when the node enters fix/close.

- comment_id: H-003
- status: non_blocking
- comment: `artifacts/data_lineage.yaml` records `redistribution_allowed: unknown_for_large_local_h5_pack`. This is an honest and acceptable limitation for the current stage. Before formal publication, the redistribution status should be resolved with the data provider.
