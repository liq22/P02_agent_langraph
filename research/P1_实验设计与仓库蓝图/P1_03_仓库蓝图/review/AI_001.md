# AI_001 External Re-Review

- review_id: EXTERNAL-AI-001
- reviewer_agent_id: external-reviewer-subagent-p1-03-001
- reviewer_role: external_node_reviewer
- reviewed_node_path: research/P1_实验设计与仓库蓝图/P1_03_仓库蓝图
- rubric: prompts/review_rubric.yaml
- rubric_version: nature_node_review_v1
- independence_confirmed: true
- overall_score: 84
- overall_verdict: pass
- hard_fail: false
- downstream_ready: true

## Summary

The author fixes close P1_03_REVIEW_B01. The revised package now path-binds `OperatorRegistry` through `configs/operators/*.yaml`, separates PHMGA workflow graph configs under `configs/workflow_graphs/*.yaml`, adds `planner_normalization_trace.json` to the required run bundle, and requires `artifact_index.json` plus validation gates to preserve planner trace provenance. It also hard-blocks scheduler graph, Canvas, dashboard, or web projection files from serving as experiment DAG inputs.

The node remains correctly scoped as a repository blueprint, not an execution result. It still keeps unresolved RM101 reject evidence, unlocked `selected_global_best_backend`, pending PHMGA/Vibench adapter preflight, and missing Stage C/D rows visible rather than promoting them into positive paper claims.

## B01 Recheck

P1_03_REVIEW_B01 is closed.

Evidence:

1. `docs/manuscript.md:40-48` adds `configs/operators/*.yaml` and `configs/workflow_graphs/*.yaml` to the minimal directory plan.
2. `docs/manuscript.md:98-108` defines run config, operator registry, workflow graph config, and `workflow_graph_path` boundaries.
3. `docs/manuscript.md:118` requires `planner_normalization_trace.json` in the run bundle and linked from `artifact_index.json`.
4. `artifacts/repo_blueprint.yaml:28-37` defines operator and workflow graph config ownership/truth boundaries.
5. `artifacts/repo_blueprint.yaml:78-90` includes `planner_normalization_trace.json` in the required run bundle.
6. `artifacts/repo_blueprint.yaml:109-117` adds artifact, operator registry, and workflow graph quality gates.
7. `artifacts/repo_blueprint.yaml:142-162` defines the run config contract, including `workflow_graph_path` and `operator_registry_path`.
8. `artifacts/module_map.yaml:27-54` path-binds planner and compiler inputs and validation obligations.
9. `artifacts/module_map.yaml:115-145` adds operator registry, workflow graph, and run bundle trace contracts.

No blocking issues remain.

## Rubric Scores

| Dimension | Score | Rationale |
| --- | ---: | --- |
| originality_novelty | 80 | The contribution is mainly disciplined architecture, but it materially improves evidence hygiene for this pipeline. |
| scientific_importance | 84 | The blueprint addresses reproducible formal-result generation and prevents common evidence inflation paths. |
| evidence_technical_soundness | 84 | Claims are now mapped to design artifacts, explicit contracts, and failure gates without promoting unresolved experiment rows. |
| reproducibility_transparency | 86 | Data, config, operator registry, workflow graph, planner trace, metrics, ledger, and artifact paths are inspectable. |
| broad_interest_story_clarity | 86 | Required vs deferred modules and result-truth boundaries are clear for reviewers outside the implementation details. |
| review_robustness | 88 | Negative evidence, reject rows, missing preflight, duplicate truth, secret leaks, operator gaps, and scheduler graph confusion are visible and gated. |

Weighted overall score: 84/100.

## Acceptance Check

- Minimal module boundaries: pass. The seven implementation modules remain minimal; operator registry and workflow graph are config inputs, not hidden extra modules.
- Reproducible result-generation path: pass. The dataset-to-ledger path now includes operator registry, workflow graph, planner trace, validation, and ledger contracts.
- Script/config/artifact locations: pass. Entrypoints, configs, run bundle, artifact index, metrics, and ledger locations are clear.
- Duplicate result truth prevention: pass. Metrics and table eligibility remain constrained to `metrics.json` and the ledger.
- Negative/reject evidence visibility: pass. RM101 reject bundles, unlocked backend selection, pending adapter preflight, and missing Stage C/D rows remain visible.
- No promotion of unresolved PHMGA evidence: pass. P1_03 remains a blueprint and explicitly forbids formal performance readiness claims from this node.

Verdict: pass. This means P1_03 is ready for downstream handoff as a repository-blueprint node; it is not a final submission-readiness claim.
