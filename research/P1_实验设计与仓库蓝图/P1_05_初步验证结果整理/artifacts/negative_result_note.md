# Negative Result and Limitation Note

P1_05 keeps two negative or uncertain outcomes in the evidence record. First, P1_04 does not provide real-data or formal Stage C/D evidence, so any broad performance claim remains unsupported. Second, the synthetic supervisor_proving gain is a single-run result, so variance stability is unclear.

The interpretation is intentionally conservative: the bounded synthetic result can justify keeping the mechanism as a candidate, but it cannot justify a submission-level performance claim, selected-backend claim, RM101-resolution claim, or real-data generalization claim. These limitations remain visible in `result_registry.yaml`, `hypothesis_status.yaml`, and `claim_evidence_registry.yaml`.
