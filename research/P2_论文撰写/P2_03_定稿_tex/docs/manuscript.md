# P2_03 TeX Synchronization Note

This node synchronizes the current manuscript state into an exportable TeX article. It does not declare the whole paper submission-ready.

## Venue Format Key Items

The current export target is a Nature-style article boundary: abstract, introduction, evidence model, methods, results, discussion, data availability, and code availability must be visible in `tex/main.tex`. The manuscript also keeps the P2_03 section hierarchy and availability statements explicit so later venue-specific formatting can be applied without changing scientific content.

## Markdown Specialization

The section map points to four Markdown sources. The Methods source is substantive and was synchronized into `tex/sections/method.tex`. The Introduction, Preliminary, and Experiment/Discussion sources were not substantive at synchronization time, so P2_03 generated conservative TeX sections that disclose the evidence boundary rather than pretending the source manuscripts were complete.

## Current Output

This round produced:

- `tex/main.tex`
- `tex/sections/introduction.tex`
- `tex/sections/preliminary.tex`
- `tex/sections/method.tex`
- `tex/sections/experiment.tex`
- `artifacts/export_constraints.yaml`
- `artifacts/tex_sync_plan.yaml`
- `artifacts/claim_evidence_registry.yaml`
- `artifacts/citation_registry.yaml`
- `artifacts/tex_compile_report.yaml`
- `artifacts/submission_evidence_gap_matrix.yaml`

The quantitative evidence carried into the TeX article now has three streams: the P1_04/P1_05/P1_09 preliminary synthetic/offline single-run evidence appears as a table with a label, caption, claim_ref, evidence_ref, and provenance note; the PHMGA Stage B backend-comparison record is summarized as a bounded real-data table with two accepted Ottawa rows while the matching RM101 rows remain rejected; and the RM101 all-domain mixed thesis case is reported as a simulated-planner mechanism case, with the strongest row explicitly marked as simulated rather than online API evidence. The manuscript explicitly blocks cross-dataset performance, accepted RM101 Stage B resolution, selected-backend readiness, Stage C/D success, and final submission-readiness claims.

This revision also normalizes the Methods and Discussion prose. The Methods section now describes the comparator set and audit procedure without implying that unavailable comparator rows have already been completed. The Discussion separates process evidence from performance evidence, explains how the RM101 case supports a mechanism-level innovation claim, and lists the concrete evidence still needed before a submission-level empirical claim can be made.

The Discussion now includes an evidence-to-claim synthesis table. This table is intended for skim readers and reviewers: it shows which innovation claim each evidence layer can strengthen, which boundary remains active, and what evidence is still missing before a stronger empirical claim can be made.

The Introduction now states the same framing earlier in the paper as three bounded contributions: node-level claim-evidence contracts, ledger-based failure retention, and an RM101 simulated-planner mechanism hypothesis. This makes the claimed innovation visible before the Results while preserving the same evidence limits.

The article now includes a Limitations section. It separates construct validity, internal validity, external validity, and conclusion validity so that the current process-auditability claim is not confused with an unearned final diagnostic-performance claim.

The abstract and conclusion were rewritten as a matched pair. The abstract now follows problem, method, evidence, and boundary. The conclusion closes on claim-grounding and mechanism evidence, then states the empirical conditions required before a stronger performance argument can be made.

This pass also normalizes caption and availability wording. Audit identifiers remain visible, but captions now use an "Audit trail" phrase rather than raw log-style `claim_ref` / `Source/provenance` language, and availability statements describe repository-local evidence packages in publication-facing terms.

This pass also adds a submission evidence gap matrix for the PHMGA state. It records which claims the current Stage B and RM101 simulated-planner evidence can support, which claims remain blocked, and what local artifacts must close before the manuscript can move from a bounded process article to a performance-centered submission.

This pass tightens publication-facing prose in the abstract, introduction, results lead-ins, discussion, and conclusion. The revision reduces workflow-log phrasing, separates auditability claims from diagnostic-performance claims earlier in the reader path, and keeps the PHMGA and RM101 evidence boundaries unchanged.

This pass also strengthens the innovation-to-evidence path. The Methods section now states the observable evidence condition and retained failure condition for claim-grounding, failure-retaining ledgers, and physics-aware DAG mechanism evidence before the Results are interpreted. The Discussion synthesis table now names the innovation claim tested by each evidence layer.

This pass adds a compact literature-positioning paragraph in the Introduction. It cites ReAct, Reflexion, and Toolformer as prior language-agent capability work, then narrows AutoResearch to downstream manuscript claim-evidence gating rather than claiming a new reasoning, feedback, or tool-use agent.

This pass reduces internal project-log wording in the Introduction, Results lead-ins, captions, and Data availability. Audit identifiers and provenance obligations remain visible, but the surrounding prose now uses publication-facing descriptions rather than upstream-node names as the primary reader path.

This pass renames the reader-facing `Preliminaries` section to `Evidence Model` and changes the evidence-to-claim synthesis table to ragged-right paragraph columns. The source file remains `tex/sections/preliminary.tex`, but the article heading now better matches the section's role and the table no longer relies on fully justified narrow columns.

This pass also adds a PHMGA Stage B pending closure contract. Local preflight passes for `ottawa_ml_codex_v3`, `rm101_ml_codex_v3`, and `rm101_ml_openrouter_glm_v2`. A sandboxed `ottawa_ml_codex_v3` full run emitted complete artifacts and metrics, but it remains `needs_rerun` because the Codex provider path fell back after `LLMProviderError` / read-only filesystem initialization failure. The Codex v3 Ottawa/RM101 pair remains the next highest-value execution target, but it requires an explicitly authorized clean provider run before it can support selected-backend closure.

This pass applies another publication-prose calibration to the abstract, Introduction, Results, Discussion, and Conclusion. It replaces workflow-log expressions such as "submission fitness", "first manuscript callout", and "research-operation discipline" with more conventional manuscript language, while preserving the same PHMGA Stage B, RM101 simulated-planner, selected-backend, Stage C/D, and variance boundaries.

This pass folds the `ottawa_ml_codex_v3` needs-rerun evidence into the Stage B manuscript path. The Results table now reports it as a pending Ottawa Codex row with emitted artifacts and metrics, while explicitly retaining the provider-fallback boundary and non-selection status. The change strengthens the failure/pending-evidence retention claim without upgrading selected-backend, RM101, Stage C/D, or cross-dataset performance claims.

This pass also states the Stage B status distribution directly in the Results: eight active rows resolve to two accepted, three rejected, and three pending rows. The manuscript table remains a metric/artifact-bearing subset, but the surrounding paragraph uses the full distribution to support the status-preservation claim rather than a backend ranking.

This pass calibrates the Evidence Model and Methods language. It replaces internal workflow terms such as `schedulable unit`, `author exit`, and `human-gate` with publication-facing terms such as accounting unit, draft completion, evidence closure, and review or human-approval status. It also updates the Methods contribution criteria so accepted, rejected, and pending PHMGA rows all contribute to the status-preservation claim without upgrading any performance claim.

This pass sharpens the reader-facing title, abstract, introduction contribution paragraph, discussion opening, and conclusion. The title now foregrounds claim-grounded evidence ledgers; the abstract and discussion make the PHMGA Stage B ledger the evidence case for status preservation; and self-referential phrases such as "current article" and "present article" were reduced without changing evidence strength.

This pass removes the internal date from the article title block, replaces remaining "review gate" phrasing with review-criteria language, and rewrites Data and Code availability as publication-facing artifact provenance statements rather than repository operation notes.

This pass removes the internal audit-rubric path from the TeX bibliography. The method now describes it as a predefined audit rubric retained with the reproducibility artifacts, while the citation registry keeps the local source as a reproducibility artifact rather than a reader-facing bibliography item.

This pass performs a publication-voice polish without changing evidence strength. The abstract now reports the PHMGA Stage B status distribution and the Ottawa Codex needs-rerun metric directly as status-preservation evidence; the Introduction states the contribution as methodological rather than as a new agent or finished diagnostic model; Results captions use repository-provenance language instead of audit-log wording; and Discussion, Limitations, and Conclusion now distinguish manuscript discipline from diagnostic-performance claims in a more reviewer-facing style.

This pass rewrites Evidence Model and Methods as a case-study audit protocol. The text now uses `evidence unit`, source-ledger status, evidence boundary, and claim-status preservation as the main method terms. It removes the earlier comparator wording that could imply a completed comparative trial, and makes the primary audited outcome the preservation of accepted, rejected, and pending source status in the manuscript.

This pass strengthens the result-to-innovation bridge in the Discussion synthesis table. The table now begins each row with the observed empirical signal: synthetic/offline macro-F1 movement, PHMGA Stage B accepted/rejected/pending status distribution with reported macro-F1 values, and the RM101 real-baseline versus simulated-planner macro-F1 pair. The same row then states the supported contribution, retained boundary, and missing evidence.

This pass removes remaining project-log wording from the Results section. The preliminary synthetic/offline table now uses reader-facing evidence-row labels instead of internal workflow IDs; the Stage B table uses review-oriented gate descriptions rather than qualityfix or need-replan/need-patch language; and the final Results paragraph replaces adapter/review-threshold wording with implementation-preflight and independent-review evidence.

This pass rewrites Limitations and Conclusion to reduce template-like repetition. The limitations section now explains construct, internal, external, and conclusion limits as consequences of the current evidence state, and the conclusion now states more directly that the PHMGA evidence supports status preservation and mechanism formation, not a closed diagnostic-performance story.

This pass rewrites Data and Code availability into a cleaner reproducibility statement. It names the local reproducibility package, PHMGA worker-result ledgers, `paper_phmga` snapshot, RM101 simulated-planner package, manuscript source, validation scripts, export maps, deterministic figure renderer, and PHMGA submodule snapshot, while keeping raw external/private data excluded and treating graph/canvas/dashboard views as non-evidence visualizations.

This pass tightens the abstract and opening Introduction to reduce repeated agent/evidence framing. The opening now states the problem as manuscript narrative closing before experiment evidence justifies closure, and the contribution is described as claim-status preservation rather than a broad process/documentation claim.

This pass rewrites the Evidence Model, Methods, Results close, Discussion, Limitations, and Conclusion for a less template-like publication voice. The Stage B Codex v3 metric is now used as the concrete example of why source status matters: an attractive macro-F1 value remains pending when the provider path falls back. The revision strengthens the result-to-innovation bridge while preserving the PHMGA boundaries: selected-backend readiness, RM101 Stage B resolution, Stage C/D success, statistical significance, and cross-dataset performance remain unclaimed.

This pass smooths the article-level reader path. The abstract now leads with the unresolved-evidence failure mode, then names the three evidence sources and the Ottawa Codex v3 needs-rerun row as the central source-status conflict. The Introduction replaces a mechanical contribution list with three bounded commitments, and the Results now states that the Stage B ledger provides evidence for the methodological claim precisely because a high-scoring row remains pending under its provider-fallback boundary. No selected-backend, RM101, Stage C/D, variance, or cross-dataset performance claim was upgraded.

This pass further compresses template-like limitation and provenance language. The Methods procedure now reads as an operational audit rather than a generic rule statement; Results captions use shorter provenance wording; the missing-evidence paragraph is written around the concrete absences that control allowable claims; and Data/Code availability now refers to the reproducibility package directly. The PHMGA boundaries remain unchanged.

This pass revises the evidence-to-claim synthesis into a design-check table. The second column now names the AutoResearch behavior exercised by each result: weak positive evidence kept below benchmark strength, source status controlling a high-scoring pending Codex row, and a simulated RM101 gain converted into a physics-aware DAG hypothesis. The Results closing paragraph was aligned to the same three design checks without upgrading any performance claim.

This pass makes the Introduction and Discussion more evidence-first. The Introduction now frames the problem as manuscript prose sounding settled while source rows remain accepted, rejected, or pending. The Discussion now starts from the PHMGA mixed frontier itself: accepted Ottawa rows, rejected RM101 rows, and a high-scoring pending Codex row that would invite a backend-ranking claim if the provider-fallback boundary were removed. The claim remains status preservation, not diagnostic benchmarking.

This pass retitles the article around incomplete experimental evidence and rewrites the abstract, Introduction, Methods, Results, Discussion, and Conclusion toward a more conventional submission voice. The evidence-to-claim table now reports the strongest conservative inference for each stream rather than a broad design slogan, and the Stage B table uses evidence-check wording instead of internal gate language. PHMGA selected-backend, RM101 Stage B, Stage C/D, variance, and cross-dataset-performance boundaries remain unchanged.

This pass further aligns the experiments with the innovation claim by defining three promotion risks across the abstract, Introduction, Methods, Results, and Discussion: weak positive proxy evidence becoming benchmark evidence, a pending high-scoring backend row becoming a selected backend, and a simulated RM101 mechanism signal becoming online diagnostic performance. The revision keeps all three as status-preservation checks and does not upgrade any PHMGA or RM101 performance boundary.

This pass softens remaining system-description language. The Introduction now states the three contributions as author claims rather than a mechanical list, the Methods section describes PHMGA as a case study of evidence moving into a manuscript, and the Discussion/Threats/Conclusion use disciplined-reporting language instead of broader auditability slogans. Evidence strength and PHMGA boundaries remain unchanged.

This pass rewrites the Evidence Model and Methods definitions for a more natural manuscript voice. The evidence unit is now introduced as the bridge between experiments and claims rather than as a system record, claim records are described by their role in limiting allowable statements, and the Methods opening explains why PHMGA's mixed accepted/rejected/pending evidence makes it an informative case. No claim boundary or PHMGA result status was changed.

This pass tightens conclusion validity language by replacing a comparative "improve auditability" claim with the supported statement that evidence-unit contracts can preserve claim status during writing. This avoids implying a completed comparative auditability experiment.

This pass shortens the title to avoid repeated "evidence" wording and rewrites Data/Code availability in artifact-package language. It removes branch-oriented and UI-surface phrasing from the reader-facing availability statements while preserving the same provenance boundary and not claiming public archival availability.

This pass applies a submission-tone and result-to-innovation polish. The abstract now names three ways evidence can be overstated, the Introduction and Methods reduce conversational/process wording, the Results state the Codex v3 row as a source-status conflict, and Data availability now refers to the manuscript rather than a draft. No PHMGA selected-backend, RM101 Stage B, Stage C/D, variance, or cross-dataset-performance claim was upgraded.

This pass narrows the Nature/IEEE citation use in the Introduction. The policy citations now support only transparent-reporting, disclosure, and peer-review-accountability context, and the citation registry marks the two entries as verified governance context rather than broad support for evidence-discipline claims. No empirical claim was changed.

This pass rewrites the Evidence Model section for a less system-descriptive voice. The evidence unit is now introduced as the accounting unit between experiment and claim, claim records answer the concrete question of the strongest statement allowed by the current evidence state, and the method contribution is stated as preserving distinctions among accepted, rejected, and pending rows. The PHMGA selected-backend, RM101 Stage B, Stage C/D, variance, and cross-dataset-performance boundaries remain unchanged.

This pass rewrites Limitations from a template-like validity-category summary into concrete reviewer-facing limits. It now states that the case must not be read as diagnostic-model superiority, that the Ottawa Codex score may reflect provider fallback rather than a clean configured-backend run, that the RM101 simulated result may reflect mixed-domain structure rather than leave-domain-out generalization, and that absent Stage C/D and statistical evidence still block selected-backend and reliability claims.

This pass rewrites the Conclusion so the final paragraph closes from the three observed evidence-status checks rather than from a generic no-performance boundary. It now states that the synthetic/offline result remains a proxy, the high-scoring Ottawa Codex row remains pending after provider fallback, and the RM101 simulated-planner result remains a mechanism hypothesis. The conclusion keeps the same RM101 Stage B, Stage C/D, repeated-run variance, and performance-submission blockers.

This pass rewrites the Abstract into a more result-driven submission form. It now names the three evidence streams directly, reports the Stage B status distribution, identifies the Ottawa Codex 0.8775 macro-F1 row as pending rather than selected-backend evidence, and closes on claim-status preservation rather than broad empirical performance. No PHMGA selected-backend, RM101 Stage B, Stage C/D, repeated-run variance, or submission-level performance claim was upgraded.

This pass rewrites the Introduction contribution paragraph to reduce list-like template wording. The contributions now name the evidence-unit representation, the PHMGA Stage B ledger analysis that keeps accepted/rejected/pending evidence in one manuscript path without becoming a backend ranking, and the bounded RM101 simulated-planner mechanism hypothesis. Evidence strength and all PHMGA/RM101 blockers remain unchanged.

This pass rewrites the Discussion synthesis table from a generic conservative-inference table into a result-to-claim upgrade table. Each evidence stream now states the stronger claim it blocks, the limited claim currently allowed, and the evidence still needed. The surrounding Discussion also explains why the Ottawa Codex 0.8775 row is the clearest support for the ledger method: the same metric that would rank highly remains pending when provider provenance fails. No selected-backend, RM101 Stage B, Stage C/D, variance, or cross-dataset-performance claim was upgraded.

This pass rewrites Methods to reduce process-manual language. PHMGA is now introduced as a mixed-evidence case study, the analysis is stated as a four-step transfer from result records to manuscript claims, and each innovation claim is tied to an observable condition before Results. The revision removes repeated closure/status procedure wording while keeping the same selected-backend, RM101 Stage B, Stage C/D, variance, and cross-dataset-performance boundaries.

This pass rewrites Results to reduce ledger/provenance log wording in the reader-facing text. The section now leads with the numerical signals and the limited conclusions they allow: the synthetic/offline gain remains a proxy, the Ottawa Codex 0.8775 macro-F1 row remains pending because of provider fallback, and the RM101 simulated-planner gain remains a mechanism candidate. Captions were shortened to retain only the claim boundary needed by readers. No selected-backend, RM101 Stage B, Stage C/D, variance, or cross-dataset-performance claim was upgraded.

This pass rewrites Limitations and Conclusion in a less template-like form. The limitations now identify the main misreading directly: the PHMGA rows test manuscript claim boundaries, not diagnostic-model superiority. The conclusion now closes around the three blocked claim upgrades: proxy to benchmark evidence, pending Codex row to selected-backend readiness, and simulated RM101 gain to cross-dataset diagnostic performance. All PHMGA selected-backend, RM101 Stage B, Stage C/D, variance, and cross-dataset-performance blockers remain unchanged.

This pass rewrites the Abstract into a more conventional problem-method-evidence-boundary structure. It now reports the Ottawa Codex 0.8775 macro-F1 row as pending evidence and adds the RM101 simulated-planner 0.8865 versus 0.8229 baseline contrast as mechanism evidence, while closing on proxy/accepted/rejected/pending/mechanism-level evidence distinctions. No selected-backend, cross-dataset-performance, Stage C/D, or submission-level empirical-performance claim was upgraded.

This pass rewrites the Evidence Model section to reduce system terminology. The evidence unit is now introduced as the link between an experimental result and the manuscript statement written from it, claim records are framed around the strongest sentence, table entry, or caption the evidence can support, and conservative labels are explained as accepted, rejected, pending, proxy, and mechanism-level evidence states. All PHMGA selected-backend, RM101 Stage B, Stage C/D, variance, and cross-dataset-performance boundaries remain unchanged.

This pass rewrites the Introduction contribution framing to reduce list-like "bounded contributions" language. The introduction now presents AutoResearch as a method for carrying evidence status into manuscript writing, then connects the evidence-unit representation, PHMGA Stage B ledger, and RM101 simulated-planner case in one narrative. The PHMGA evidence status remains unchanged: Ottawa accepted rows, RM101 rejected rows, and the pending Codex row are not upgraded into a backend ranking or diagnostic-performance claim.

This pass rewrites the Discussion opening around source authority rather than defensive limitation language. The PHMGA Codex row is now framed as a disagreement between score and provenance: the metric would look selection-relevant in a score-only comparison, but the provider fallback keeps it pending and selection-ineligible. The RM101 simulated-planner paragraph now presents the physics-aware DAG result as a bounded hypothesis for formal testing rather than a repaired RM101 backend result. No selected-backend, RM101 Stage B, Stage C/D, variance, statistical-significance, or cross-dataset-performance claim was upgraded.

This pass rewrites Methods from a procedural checklist into a case-study design description. The section now explains why the uneven PHMGA evidence state is useful, how source status constrains each manuscript statement, and how the three innovation claims are evaluated through source-status preservation. The claim registry was aligned to this wording. No experimental result, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, statistical claim, or cross-dataset-performance claim was changed.

This pass rewrites the Abstract for a less template-like submission voice. The abstract now states the reporting failure, evidence-unit response, PHMGA uneven evidence case, Ottawa Codex source-status conflict, and RM101 simulated-planner mechanism signal in one problem-method-case-boundary flow. It replaces the previous "bounded claim" phrasing with a manuscript-level contribution statement and keeps selected-backend readiness, cross-dataset performance, and submission-level empirical performance unclaimed.

This pass rewrites the Conclusion to close around the manuscript-level contribution rather than template-like "claim upgrade" language. The final paragraph now states that the unfinished PHMGA record tests whether claims remain tied to evidence status, then names the three retained boundaries: synthetic/offline evidence as proxy, Ottawa Codex 0.8775 macro-F1 as pending after provider fallback, and RM101 simulated-planner gain as mechanism hypothesis. The claim registry was aligned to this wording; selected-backend, RM101 Stage B, Stage C/D, variance, and cross-dataset-performance blockers remain unchanged.

This pass rewrites Limitations from validity-category prose into concrete risk paragraphs. The section now starts with scope confusion, then separates sparse/simulated evidence, the Ottawa--RM101 transfer limit, and missing closure evidence. It keeps the same boundaries: PHMGA is not treated as a diagnostic-model comparison, the Codex row remains pending after provider fallback, the RM101 simulated row remains a candidate for a formal RM101 test, and Stage C/D plus repeated real-data variance remain missing.

This pass rewrites the Results transitions so the section reads less like a sequence of cases and more like a source-status interpretation of three evidence streams. The synthetic/offline row is now introduced as a test of whether a favorable proxy signal remains below benchmark strength, the Stage B ledger is framed as the real-data authority-inversion check for accepted/rejected/pending evidence, and the RM101 mixed experiment remains a separate mechanism signal. No metric, table, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, or cross-dataset-performance claim was changed.

This pass rewrites the final Introduction paragraph to remove internal project-history wording. The paragraph now presents the case as an uneven evidence state: traceable synthetic/offline material, accepted Ottawa Stage B rows, rejected RM101 rows, a high-scoring Ottawa Codex row pending after provider fallback, and a simulated RM101 physics-aware DAG signal. It preserves the same boundary: the paper tests claim discipline under incomplete evidence, not selected-backend readiness or cross-dataset diagnostic performance.

This pass rewrites the Evidence Model section to sound less like a system definition. Evidence units are now introduced as the smallest reportable link between a result and the claim drawn from it, and manuscript statements are described as inheriting source status. The section now explains accepted, rejected, pending, proxy, and simulated evidence as writing constraints before applying them to PHMGA. The C01 claim registry entry was aligned to this protocol/artifact/source-status/boundary/closure wording, with all PHMGA evidence boundaries unchanged.

This pass rewrites Data and Code availability to remove graph/dashboard-oriented project wording. The availability statements now say that reported values are traceable to accompanying materials, that raw external or private datasets are not redistributed, and that only files that generate the reported tables plus validation scripts are used as claim evidence. The change preserves the same PHMGA, RM101, and submission-readiness boundaries.

This pass tightens the Abstract, Methods closure paragraph, Limitations, and Conclusion around a reviewer-facing reporting-method claim. The Abstract now calls the contribution methodological rather than manuscript-level. Methods replaces repository/local-record wording with audited-evidence-set language, separates manuscript completion from evidentiary closure, and lists only the minimum provenance identifiers needed for audit. Limitations now frames sparse and simulated rows as overstatement risks controlled by the method. The Conclusion states the supported claim directly: AutoResearch preserves claim status under incomplete evidence, while diagnostic-performance submission still requires accepted Ottawa--RM101 backend evidence, Stage C/D evidence, and repeated real-data runs with variance estimates.

This pass strengthens the experiment-to-innovation path. The Results close now states how the three evidence streams exercise the method: the synthetic/offline row tests whether a positive proxy remains below benchmark strength, the Stage B ledger tests whether accepted/rejected/pending rows remain distinct when score and provenance disagree, and the RM101 mixed case tests whether a physics-aware DAG signal remains a mechanism candidate. The Discussion synthesis table was rewritten as a result-to-innovation table with method behavior tested, stronger claim withheld, and evidence still needed. No PHMGA selected-backend, RM101 Stage B, Stage C/D, variance, or diagnostic-performance claim was upgraded.

This pass polishes the reader entry. The title now foregrounds preserving claim status under incomplete experimental evidence rather than claim-grounded ledgers. The abstract first sentence now describes the promotion of numerical results beyond their evidentiary status, and the contribution sentence states status preservation directly. The Introduction opening and contribution paragraph were revised to reduce prose/manuscript repetition and to state that AutoResearch carries evidence status into the claim itself. No PHMGA metric, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, or diagnostic-performance claim was changed.

This pass rewrites the Evidence Model definition in a more publication-facing style. The evidence unit is now described as the smallest reportable link between a result and the claim made from it, and the section states that only statements carrying a claim, table entry, or caption need this link. The status rule is phrased as the strongest statement allowed by source status, and the PHMGA paragraph now presents the case as an application of that rule under mixed evidence. Accepted, rejected, pending, proxy, and simulated boundaries remain unchanged.

This pass tightens Discussion and Limitations. The Discussion opening now separates metric magnitude from evidentiary authority and states that the Ottawa Codex score remains pending because its provenance condition failed. The Limitations section now frames the main risk as a scope mismatch: PHMGA rows may resemble a diagnostic-model comparison, but in this article they are evidence cases for claim-status preservation. The closure paragraph now states the hard boundary created by missing Stage C/D rows, repeated real-data variance, and cross-dataset accepted backend evidence.

This pass rewrites Methods as a case-study design rather than a procedure list. PHMGA is now introduced as a mixed evidence set that exposes the central failure mode: a result can have a strong metric while its admissible claim remains narrow. The section now evaluates three observable method behaviors: evidence-unit boundary retention, Stage B accepted/rejected/pending separation, and bounded RM101 mechanism-signal reporting. The C12 claim registry entry was aligned to this wording, and no PHMGA performance boundary was upgraded.

This pass fixes the Results display type for the preliminary synthetic/offline metric evidence. The first metric block is now a table, referenced as Table~\ref{tab:p1-synthetic-signal}, rather than a figure environment containing a tabular result. The TeX callout evidence in the claim registry and the tex sync plan were aligned to table terminology. No metric value, source status, or claim boundary changed.

This pass aligns Code availability with the current table-only evidence displays. The reader-facing statement now names manuscript source, validation scripts, table-generation files, and PHMGA evidence files, and removes the earlier reference to figure-rendering code. The change is presentational only and leaves all table evidence, PHMGA status boundaries, and raw-data exclusions unchanged.

This pass rewrites the Conclusion to remove residual template-like language such as "unfinished record" and "tempting upgrades." The conclusion now states the supported result directly: AutoResearch preserves claim status when an experimental record is incomplete. It then names the three bounded signals and retains the same diagnostic-performance blockers: accepted Ottawa--RM101 backend evidence, accepted Stage C/D evidence, and repeated real-data variance estimates.

This pass strengthens the experiment-to-innovation link by organizing the Results around blocked claim upgrades. The Results opening now states that each evidence stream is read by the upgrade it could invite, the Stage B paragraph names the high-scoring Codex row as the central status-preservation test, and the closing paragraph states the three refusals that support the method contribution: no proxy-to-benchmark upgrade, no pending-score-to-backend-selection upgrade, and no simulated-mechanism-to-online-performance upgrade. The Discussion synthesis table was aligned to the same claim-upgrade blocking language, and two residual template-like phrases in Methods and the RM101 caption were replaced, without changing any metric or PHMGA evidence boundary.

This pass polishes the reader entry and close. The abstract now uses a problem--method--case--boundary flow: incomplete agent-assisted records can become fluent claims before provenance and closure are checked; evidence units constrain reportable sentences and table entries; the PHMGA case supplies the uneven evidence; and the result supports status preservation rather than a diagnostic-performance result. The Introduction opening now frames the transfer step from result record to claim, and the conclusion now starts from the narrow supported PHMGA claim instead of a template-like generic study-opening construction. No selected-backend, RM101 Stage B, Stage C/D, variance, or diagnostic-performance boundary changed.

This pass rewrites the Discussion boundary paragraph and Limitations so they read as evidence-status consequences rather than defensive checklists. The Discussion now states that backend selection requires an accepted Ottawa--RM101 pair and that performance-centered claims require accepted Stage C, Stage D, and repeated real-data estimates. Limitations now frames the metric-bearing PHMGA rows as deliberate pressure on claim-status preservation, with sparse evidence, provider fallback, simulated planners, mixed-domain RM101, and missing closure evidence retained as concrete limits. No metric, table, selected-backend status, RM101 Stage B state, Stage C/D state, variance claim, or diagnostic-performance claim changed.

This pass makes the Results tables and synthesis table more reader-facing. The Stage B table now uses Evidence status and Evidence note columns with accepted/rejected/pending labels instead of raw ledger verbs, the synthetic row is labeled as a run rather than an attempt, and the Discussion table now uses "Claim withheld" under a "Claim-boundary synthesis" caption. The Results close now describes three conservative decisions rather than repeated refusals. The C07 registry and sync plan were aligned to this claim-boundary wording. No PHMGA metric, source status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, or diagnostic-performance claim changed.

This pass strengthens the Methods evaluation protocol. The method now applies the same four-part reading to each evidence stream: record the numerical result, fix source status before interpretation, state the strongest allowed claim, and pair any withheld stronger claim with the evidence needed to make it. This makes the Results tables directly auditable as tests of claim-status preservation rather than only narrative examples. The C12 registry entry was aligned to this four-part protocol. No PHMGA metric, source status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, or diagnostic-performance claim changed.

This pass smooths the Evidence Model and Methods wording after the protocol revision. The Evidence Model now avoids figure-specific and provider-fallback compound phrasing, and the Methods section now uses source-record and source-artifact language. It keeps the same evidence-unit definition, four-part evaluation protocol, and PHMGA claim boundaries.

This pass polishes the final article structure and availability statements. The reader-facing limitations section is now titled "Limitations" rather than the earlier validity title, and Data/Code availability now uses backend-comparison records, table-generation files, and files that generate the reported tables instead of ledger/mapping/artifact phrasing. The C09 registry and sync plan were aligned to the Limitations wording. No PHMGA metric, source status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, or diagnostic-performance claim changed.

This pass strengthens the experiment-to-innovation bridge in the visible TeX. The Introduction now frames AutoResearch as a reporting method; Methods names the three PHMGA streams as proxy-retention, Stage B status-separation, and RM101 mechanism-boundary tests; Results and Discussion replace reader-facing ledger wording with Stage B comparison/record wording; and the conclusion states that the evidence supports an evidence-status-preservation method contribution, not a diagnostic-performance submission. The claim registry, submission evidence gap matrix, and sync plan were aligned. No PHMGA metric, selected-backend status, RM101 Stage B state, Stage C/D state, variance claim, or diagnostic-performance claim changed.

This pass adds an evidence-status policy table to the Evidence Model. The table states the allowed manuscript use and withheld claim for accepted, rejected, pending, proxy, and mechanism-level sources before PHMGA results are interpreted. Methods now points to that table when applying the four-part reading. The claim registry and sync plan were aligned. No PHMGA metric, selected-backend status, RM101 Stage B state, Stage C/D state, variance claim, or diagnostic-performance claim changed.

This pass rewrites the Abstract and the Introduction contribution sentence for a less procedural submission voice. The abstract now uses a compact problem-method-case-boundary structure, names the Stage B status distribution and the two key metrics, and states the supported behavior as evidence-status preservation rather than diagnostic performance. The Introduction now frames AutoResearch directly as a reporting method. No PHMGA metric, selected-backend status, RM101 Stage B state, Stage C/D state, variance claim, or diagnostic-performance claim changed.

This pass closes the remaining non-blocking AI_002 citation-polish issue. The Introduction now separates reporting standards from publication policies: the former motivates transparent accounts of data, code, materials, and protocols, while the latter supports originality, disclosure, and peer-review-accountability context. The response log records AI-002-W01 as closed. No empirical claim, PHMGA metric, selected-backend status, RM101 Stage B state, Stage C/D state, or variance claim changed.

This pass strengthens the Discussion by distinguishing AutoResearch from generic conservative prose. The new paragraph explains that the method keeps the metric, source status, withheld claim, and closure condition together, so future upgrades require a changed source record rather than narrative reinterpretation. It names the open closure paths--clean provider rerun, accepted RM101 backend row, Stage C/D result, and repeated-run variance--without claiming that any path is already closed. The C07 registry wording was aligned. No PHMGA metric, selected-backend status, RM101 Stage B state, Stage C/D state, variance claim, or diagnostic-performance claim changed.

This pass adds an alternative-explanation paragraph to the Discussion. It states that Ottawa separability, Codex provider fallback, and RM101 mixed-domain structure remain possible explanations for the observed signals, and that AutoResearch keeps those explanations attached to withheld claims and closure conditions rather than adjudicating them from incomplete evidence. The C09 registry and sync plan were aligned. No PHMGA metric, selected-backend status, RM101 Stage B state, Stage C/D state, variance claim, statistical claim, or diagnostic-performance claim changed.

This pass strengthens the Results-to-innovation link. The Stage B interpretation now states that the status outcome is source-status authority over score ordering: accepted Ottawa rows can be reported, rejected RM101 rows stay negative, and the pending Codex score cannot enter selection until its provider-path condition is repaired. The Results close now maps the three streams to the Methods failure modes: proxy below benchmark strength, pending score outside backend selection, and simulated RM101 DAG signal as a target for a formal RM101 run. The C05/C12 registry entries and sync plan were aligned. No PHMGA metric, selected-backend status, RM101 Stage B state, Stage C/D state, variance claim, statistical claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass names the three result-to-claim failure modes in Methods and carries those names into Results and the Discussion synthesis table. The manuscript now treats the evidence streams as tests against proxy promotion, authority inversion, and mechanism inflation. This makes the innovation claim more operational: the method is supported when those claim-promotion errors are blocked without hiding the underlying metric. The C07/C12 registry entries and sync plan were aligned. No PHMGA metric, selected-backend status, RM101 Stage B state, Stage C/D state, variance claim, statistical claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass carries the named claim-promotion errors into the paper entry and close. The abstract now states that the case blocks proxy promotion, authority inversion, and mechanism inflation; the Introduction binds the synthetic/offline, Stage B, and RM101 evidence streams to those three tests; and the Conclusion closes on those blocked errors rather than on a generic cautious-reporting claim. The C08/C10 registry entries and sync plan were aligned. No PHMGA metric, selected-backend status, RM101 Stage B state, Stage C/D state, variance claim, statistical claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass aligns the Discussion synthesis table with the same terminology. The table lead-in, second-column header, and caption now state that each empirical stream blocks a named claim-promotion error, while the withheld claim and missing evidence remain visible. This removes the more generic "method behavior" wording without changing any metrics or PHMGA evidence boundaries.

This pass applies a small publication-voice polish to Discussion and Limitations. The Discussion now frames the difference from cautious prose as an operational source-record distinction, and the sparse-evidence limitation is stated as material to the evaluation rather than as a rhetorical caveat. No metrics, claim-promotion errors, PHMGA evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, statistical claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass tightens the reader entry. The Introduction opening now states the problem as source-status loss during the transfer from result record to manuscript claim, rather than as a generic risk in agent-assisted writing. The abstract also replaces an ambiguous "it" with "this design" when introducing the PHMGA evaluation. Reporting-governance citations, claim-promotion-error framing, and all PHMGA evidence boundaries remain unchanged.

This pass improves bibliography presentation. The Nature and IEEE governance references now use short clickable link text instead of displaying long URLs in the bibliography; the full source locators remain in the citation registry. This is a typesetting change only and does not alter citation role, claim support, PHMGA metrics, or evidence boundaries.

This pass sharpens the literature-positioning paragraph. Instead of saying only that AutoResearch differs from language-agent capability work, the Introduction now states that ReAct, Reflexion, and Toolformer concern action, feedback, and tool use, whereas AutoResearch controls the admissibility of manuscript claims after candidate artifacts have been produced. The C13 registry wording was aligned. No citation role, PHMGA metric, claim-promotion-error framing, or evidence boundary changed.

This pass normalizes Evidence Model terminology. The section now uses mechanism-level evidence consistently instead of alternating with simulated evidence in the status vocabulary, and it replaces awkward claim-carrier wording with prose-claim/table-entry/caption language. The Methods cross-reference sentence was aligned. No evidence-status category, PHMGA metric, or claim boundary changed.

This pass tightens Data and Code availability. The statements now distinguish derived metrics from raw data, identify the three table sources at a publication-facing level, and state that only table-producing files plus validation scripts serve as claim evidence. Planning files, display views, and project-operation artifacts remain excluded. No data-sharing claim, PHMGA metric, or evidence boundary changed.

This pass sharpens the argument voice in Methods, Results, Discussion, and Limitations. The Methods now separates readability from evidentiary closure, the Results close frames the three streams as evidence for the reporting method, and the Discussion/Limitations replace cautious-narrative phrasing with source-status and closure-condition reasoning. No PHMGA metric, source status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, statistical claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass tightens the abstract into a problem-method-evidence-boundary structure. It preserves the PHMGA Stage B status distribution, the Ottawa Codex 0.8775 pending-row boundary, and the RM101 0.8229-to-0.8865 simulated-planner boundary while reducing stacked procedural wording. No selected-backend, cross-dataset, Stage C/D, variance, or submission-level empirical-performance claim changed.

This pass tightens the Introduction contribution path. The later Introduction paragraphs now state AutoResearch as a reporting method whose mechanism is to carry evidence status into claims, then present PHMGA as the evaluation case because high metrics sit next to unresolved source conditions. The synthetic/offline, Stage B, and RM101 streams still map to proxy promotion, authority inversion, and mechanism inflation without changing any PHMGA metric or claim boundary.

This pass tightens the Results opening. The synthetic/offline stream now enters as the proxy case in a result-overstatement sequence, and the 1.0000 offline score is framed as a narrow reporting observation rather than a performance result. This strengthens the proxy-promotion evidence path without changing any metric, Stage B status, RM101 boundary, or performance claim.

This pass removes residual internal-review wording from reader-facing Results, Discussion, Limitations, and Data availability. The Stage B paragraph now uses "status outcome" rather than audit phrasing, the Discussion contrasts operational source records with stylistic summaries, the Limitations section names the test tension directly, and Data availability refers to checking evidence boundaries. No PHMGA metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, statistical claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass tightens the Methods claim-test paragraph. The former numbered reading is now a compact claim test: numerical result, source status before interpretation, strongest allowed claim, and withheld claim with closure evidence. The three claim-promotion errors and their mapping to the synthetic/offline, Stage B, and RM101 streams are unchanged.

This pass tightens the Introduction, Discussion, Limitations, and Conclusion around the PHMGA evidence conflict. The Introduction now names the Ottawa Codex 0.8775 pending row and the RM101 0.8865 versus 0.8229 simulated-planner contrast before the Results, while the Discussion and closing sections frame the contribution as an operational source-record distinction rather than a style of cautious prose. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass sharpens the experiment-to-innovation bridge in the Evidence Model, Methods, and Results. The PHMGA case is now described as mixed evidence rather than a stress-test slogan; the Stage B paragraph explicitly serves as the real-data check for authority inversion; and the Results close maps the synthetic/offline, Stage B, and RM101 streams to proxy-promotion, authority-inversion, and mechanism-inflation blocking. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites the abstract into a cleaner problem-method-evidence-boundary paragraph. It replaces the remaining stress-case wording with the Ottawa Codex source-status conflict, states the three blocked claim upgrades as proxy promotion, authority inversion, and mechanism inflation, and keeps the RM101 0.8865 versus 0.8229 simulated-planner contrast bounded as mechanism-level evidence. The Discussion opening was also tightened to say Stage B preserves the score/status conflict rather than identifying a winning backend. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass removes a few remaining abstract and Results phrases that read like process scaffolding. The abstract now says the records show three avoided claim upgrades; the Introduction, Methods, Limitations, and Results replace process-scaffolding wording with direct source-status language. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass tightens the Introduction contribution paragraph. It now says AutoResearch keeps evidence status with the claim it licenses, and reads PHMGA through three claim-promotion errors without the earlier mechanism/contribution template phrasing. The C08 registry entry was aligned to the same language. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass polishes the Discussion synthesis table from repeated blocking phrases into claim-status decisions. The table still names proxy promotion, authority inversion, and mechanism inflation, but now frames each as the decision made for the observed evidence stream, with the withheld claim and closure evidence kept visible. The C07 registry entry and sync plan were aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass tightens the Results opening and close around the actual supported result. The opening now says the streams are organized by the claim upgrade each could invite, and the close states the narrow supported result directly: source status is retained while claims are formed. The same three blocked upgrades--proxy promotion, authority inversion, and mechanism inflation--remain tied to the synthetic/offline, Stage B, and RM101 streams. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites the Introduction contribution and PHMGA case paragraphs into a less self-referential publication voice. The contribution is stated as keeping evidence status with the claim it licenses, and PHMGA is described as the evaluation case because high metrics appear next to unresolved source conditions. The C08 registry entry was aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites Methods from a procedural checklist into a case-study design description. PHMGA is now analyzed as an uneven evidence set rather than a completed benchmark; interpretation follows source status before score magnitude; and the three evidence streams are checked against proxy promotion, authority inversion, and mechanism inflation without letting high metrics authorize stronger claims. The C12 registry entry was aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites the abstract into a more direct problem-method-case-boundary form. It replaces `claim control` with evidence-unit binding, reports the synthetic/offline 1.0000 macro-F1 proxy boundary, keeps the Ottawa Codex 0.8775 pending-row conflict, and states the supported methodological claim as source status remaining attached to claims. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites Limitations from a defensive checklist into scope-risk prose. The section now states that PHMGA's real metrics can look like a partial diagnostic-model comparison, but their role is to test whether source-status labels stay attached when metrics are reportable. It keeps sparse evidence, simulated RM101 planner rows, provider fallback, Ottawa--RM101 status mismatch, absent Stage C/D rows, and missing repeated variance as active limits. The C09 registry entry was aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites the Conclusion from a no-support checklist into a contribution close. It now separates the supported method result from open performance questions, states the three source-status decisions, and names the accepted Ottawa--RM101 backend evidence, Stage C/D rows, and repeated real-data variance estimates still required for a performance-centered submission. The C10 registry entry was aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites the post-table Discussion from rule explanation into source-record argument. The text now says source records control claim upgrades, keeps alternative explanations attached to withheld claims and closure conditions, and states the manuscript boundary through accepted Ottawa--RM101 backend evidence, Stage C/D rows, and repeated real-data estimates. The C09 registry entry was aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites the Methods claim-test language from an internal reading rule into four publication-facing decision questions: what was measured, which source verdict is attached, what claim that verdict permits, and what evidence would be required to go further. The closure paragraph now assigns closure to source verdicts and approval records rather than paragraph fluency. The C12 registry entry was aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites the Stage B Results interpretation from a metric-led ledger summary into an authority-inversion argument. The text now contrasts narrow accepted Ottawa evidence, negative RM101 evidence, and the pending high-scoring Ottawa Codex row, then states that the highest visible score is not the strongest evidence. The C05 registry entry and sync plan were aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites the Evidence Model opening from a rule-definition style into a publication-facing account of when a measured result can enter manuscript text. Evidence units now pair measured result, protocol, inspected artifact, source status, claim boundary, and closure condition before the result becomes prose, a table entry, or a caption. The PHMGA bridge now names the perfect synthetic proxy, accepted/rejected/pending Stage B split, and RM101 simulated-planner signal as sources that motivate claim-status testing without establishing performance claims. The C01 registry entry and sync plan were aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass tightens the abstract opening. It replaces the colloquial "settled-sounding manuscript claims" and first-person case sentence with a direct problem-method-case entry: agent-assisted drafting can detach manuscript claims from unresolved experiment records, and the evaluation uses PHMGA as the bounded case. The C08 registry entry and sync plan were aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass tightens the Conclusion from internal submission-gate wording into a publication-style close. The paragraph now states a narrow method result, keeps the synthetic/offline 1.0000 macro-F1 score as a proxy, the Ottawa Codex 0.8775 macro-F1 row as pending, and the RM101 simulated-planner gain as a mechanism hypothesis, then names the backend and repeated-run evidence still required for backend-readiness or diagnostic-performance claims. The C10 registry entry and sync plan were aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites Data and Code availability from repository-internal wording into a publication-facing material boundary. The statements now say raw external or private datasets are not included, identify the synthetic/offline, PHMGA Stage B, and RM101 metadata sources for derived values, and limit claim evidence to table sources, validation scripts, and PHMGA evidence records. The C02 registry entry and sync plan were aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass rewrites the Results close from an internal result-to-claim-step cue into a publication-facing synthesis. The close now states that across the synthetic/offline, Stage B, and RM101 streams the supported result is narrow: source status remains attached while claims are formed. The C07 registry entry and sync plan were aligned. No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass tightens the Introduction literature positioning. The paragraph now treats ReAct, Reflexion, and Toolformer as upstream capability work on action selection, feedback use, and tool invocation, while placing AutoResearch downstream as claim-admissibility control after candidate artifacts exist. The C13 registry entry and sync plan were aligned. No citation role, PHMGA metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass aligns the title with the current result-to-claim boundary. The reader-facing title now states source-status preservation in manuscript claims, matching the abstract, Results close, Discussion, and Conclusion claim that source status remains attached while claims are formed. The C08 registry entry and sync plan were aligned. No PHMGA metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass strengthens the RM101 result-to-innovation bridge. The Results and Discussion now state the RM101 simulated-planner evidence as a bounded contrast: two simulated variants are poor, late fusion stays near the baseline, and only the time-frequency plus load-normalization branch rises above the real baseline. The C06 registry entry and sync plan were aligned so this supports a concrete mechanism candidate rather than online performance, Stage B repair, or leave-domain-out generalization.

This pass aligns visible terminology around source-status preservation. The Introduction, Evidence Model bridge, Results Stage B interpretation, Discussion synthesis table, Limitations, and local claim registry now use source-status wording where the paper's supported method claim is source status remaining attached to manuscript claims. No PHMGA metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.

This pass removes remaining reader-facing submission-internal wording. The abstract, Evidence Model bridge, Discussion boundary, and Limitations now state the withheld endpoint as a final diagnostic performance claim rather than submission-level empirical performance or final empirical submission. The synthetic/offline table now uses reader-facing row labels, "Baseline pipeline" and "AutoResearch run." No metric, evidence status, selected-backend boundary, RM101 Stage B state, Stage C/D state, variance claim, cross-dataset claim, or diagnostic-performance claim changed.
