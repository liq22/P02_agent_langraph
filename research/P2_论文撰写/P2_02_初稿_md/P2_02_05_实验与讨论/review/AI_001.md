# External AI Review: P2_02_05 Experiments and Discussion

reviewer_agent_id: codex-external-ai-001
reviewer_role: external_node_reviewer
reviewed_at: 2026-05-05
review_scope: research/P2_论文撰写/P2_02_初稿_md/P2_02_05_实验与讨论 only, plus explicitly referenced upstream source_refs needed to verify metrics, gate blockers, and negative/unclear evidence.

## Findings

No blocking findings.

The experiments/discussion draft satisfies the node rubric at this stage. The manuscript makes the primary outcome explicit as evidence governance rather than performance improvement (`docs/manuscript.md:5`), orders results by evidence maturity (`docs/manuscript.md:7`), reports the only positive synthetic/offline row with its boundary (`docs/manuscript.md:11-13`), and keeps formal PHMGA/Vibench rows blocked rather than silently absent (`docs/manuscript.md:17-19`). The negative/unsupported/unclear evidence is visible and separated from support (`docs/manuscript.md:23-25`), and the discussion does not upgrade the single synthetic row into formal PHMGA performance, real-data generalization, RM101 resolution, Stage C/D success, selected-backend success, or submission readiness (`docs/manuscript.md:29-37`).

The metric values in the manuscript match the upstream P1_05 result registry and P1_04 TSV: `simple_fullchain` has test accuracy 0.8333333333333334 and macro-F1 0.8285714285714285; `supervisor_proving` has test accuracy 1.0 and macro-F1 1.0; the deltas are +0.16666666666666663 and +0.17142857142857149. The upstream registry labels this use as preliminary/synthetic only and forbids formal main-result, selected-backend, RM101-resolution, and submission-ready performance uses.

The local mappings are consistent with the prose. `artifacts/claim_map.yaml` separates the limited synthetic signal, formal gate blocker, negative/unclear retention, and discussion-limit claims; each claim has evidence refs, boundary labels, and forbidden wording (`artifacts/claim_map.yaml:15-101`). The local claim-evidence registry points to the necessary upstream source refs and records that unsupported, unclear, rejected, and blocked rows remain retained rather than deleted.

Graph, Canvas, dashboard, and frontend surfaces are not treated as research truth in the reviewed materials. I did not read protected paths, `.env*`, `docs/HUMAN_ONLY.md`, `_reference/**`, credentials, private keys, generated Canvas files, reports, vendor assets, PHMGA source files, Canvas/dashboard files, or unrelated node files.

## Non-Blocking Issues

- NBI-001: The manuscript states a primary manuscript outcome clearly, but the secondary outcome vocabulary is mostly implicit in the result order. Before final paper polishing, consider adding one sentence that explicitly names the limited synthetic/offline signal and formal-gate/negative-ledger rows as secondary or supporting outcomes.
- NBI-002: The prose is evidence-aligned but does not inline claim IDs. This is acceptable because `claim_map.yaml` and `claim_evidence_registry.yaml` provide the mapping, but final manuscript integration may benefit from comments or author-facing annotations that preserve the IDs through later edits.

## Verdict

Pass. No hard-fail condition is triggered. The node is downstream-ready as an experiments/discussion draft and mapping package, with the scope limited to this draft stage and without upgrading formal-result claims.
