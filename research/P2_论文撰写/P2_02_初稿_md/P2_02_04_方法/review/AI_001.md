# External AI Review: P2_02_04 Methods

reviewer_agent_id: `external_codex_reviewer_p2_02_04_2026_05_05`

reviewer_role: distinct external node reviewer

review_scope: `research/P2_论文撰写/P2_02_初稿_md/P2_02_04_方法` only, plus the explicitly named local inputs and registry source_refs needed to verify traceability.

## Verdict

Pass. The Methods prose and local registry satisfy the node rubric for a method/protocol section. The section is careful about its evidence boundary: it defines an evidence-governed workflow, not a final PHMGA result; keeps Graph, Canvas, dashboards, wrappers, and cockpit pages as projections rather than truth; and blocks performance, selected-backend, RM101, Stage C, Stage D, and submission-ready claims until the formal gates pass.

## Findings

No blocking findings.

Non-blocking issue 1: `status.yaml` and the checklist remain seed/pending even though the manuscript and registry artifacts now exist. This is not a Methods-prose hard fail and is outside the files I am allowed to edit, but it can mislead downstream operators if they use status metadata rather than the review verdict and artifacts.

Non-blocking issue 2: The registry states `registry_schema_uses_allowed_enums: true`, while the current completion audit still names P2_02_04 among global registry schema blockers. I did not treat this as a node-local hard fail because the visible P2_02_04 registry fields are coherent for the local method contract and the completion audit frames the schema issue as a broader submission validator blocker. Before final submission, the maintainer should run the repository validator and reconcile any remaining schema mismatch.

## Rubric Assessment

Methods logic is complete for this node. The prose identifies the unit of analysis, intervention, baselines, node lifecycle, author-exit versus node-close distinction, registry semantics, independent review/response closure, negative-evidence denominator policy, PHMGA/Vibench boundary, provider policy, metrics, statistics plan, appendix split, and local evidence boundary.

Key variables, settings, and assumptions are traceable. The primary metric, secondary metrics, baseline conditions, repeat-count rule, budget and statistics requirements, provider limits, PHMGA branch/commit, dirty-submodule policy, and formal-row blockers all map to `artifacts/method_contract.yaml` and upstream source refs including `protocol_map.yaml`, `table_plan.yaml`, `submodule_ref.yaml`, `venue_requirements.yaml`, and `completion_audit_current.md`.

Negative evidence is retained. The Methods section explicitly keeps negative, rejected, unclear, rate-limited, low-power, and blocked rows in denominators and forbids replacing formal rows with favorable synthetic/offline rows. This matches the table plan's `negative_unclear_result_ledger` and formal gate stop conditions.

PHMGA, Vibench, and provider boundaries are honest. Vibench is limited to catalog/read-bundle context; PHMGA owns split/windowing/DAG evaluation/ledger/table production; provider credentials are excluded; OpenRouter and BigModel lanes are limited by the free-model policy; and dirty/untracked PHMGA state blocks parent pointer updates.

The manuscript does not overclaim. It repeatedly states that this node does not report final PHMGA performance, selected backend success, RM101 resolution, Stage C/D completion, or final submission readiness. Result interpretation is correctly deferred to the experiments and discussion node.

## Required Actions

No required action for this node review gate.

Recommended follow-up before broader submission validation: refresh or reconcile stale node status/checklist metadata and resolve the global registry-schema blocker reported by the completion audit.
